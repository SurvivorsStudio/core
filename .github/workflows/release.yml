name: Release

on:
  push:
    branches: [main]
  # 최초 0.1.0 배포용. 릴리스 PR 없이 현재 버전을 그대로 publish 합니다.
  workflow_dispatch:

jobs:
  release-please:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    outputs:
      release_created: ${{ steps.release.outputs.release_created }}
    steps:
      # release-please-config.json 과 .release-please-manifest.json 을 읽습니다.
      # 매니페스트가 마지막 릴리스 버전(1.0.0)을 알려주므로, 태그가 없어도
      # 다음 버전을 1.0.0 이 아니라 1.0.x / 1.1.0 으로 계산합니다.
      - uses: googleapis/release-please-action@v4
        id: release

  publish:
    needs: release-please
    # needs 가 skip 되면 publish 도 skip 되므로 always() 로 dispatch 경로를 살려 둡니다
    if: always() && (needs.release-please.outputs.release_created == 'true' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      # npm Trusted Publishing(OIDC). 저장된 npm 토큰이 없습니다
      id-token: write
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org
      # Trusted Publishing 은 npm 11.5.1 이상이 필요합니다
      - run: npm install -g npm@latest
      - run: npm ci
      # prepublishOnly 가 build 를 돌립니다
      - run: npm publish
